{
  "name": "first",
  "version": "0.0.0",
  "description": "first",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "electron .",
    "build": "build --x64"
  },
  "author": "hans",
  "license": "ISC",
  "dependencies": {
    "electron-squirrel-startup": "^1.0.0",
    "request": "^2.79.0",
    "socket.io-client": "^1.7.1",
    "util": "^0.10.3"
  }
}
